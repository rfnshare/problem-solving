#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    // for (int i = 0; i < pivot; i++)
    // {
    //     b.push_back(a[i]);
    // }
    // for (int i = pivot + 1; i < a.size(); i++)
    // {
    //     c.push_back(a[i]);
    // }
    // if (i == pivot)
    //     {
    //         continue;
    //     }
    //     else if ()
}