#include <stdio.h>
#include <math.h>
int main()
{
    int a, b, i, j, k;
    a = 3, b = -2;
    float ans;
    ans = cos(3.141593/2);
    printf("%f\n", pow(a, b));
    printf("%f\n", sqrt(a));
    printf("%d\n", abs(b));
    printf("%f\n", ans);
}